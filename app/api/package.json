{
  "name": "@fredowisk/api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "(if not exist downloads mkdir downloads) &&  if exist downloads node src/index.js",
    "test": "set NODE_OPTIONS=--experimental-vm-modules && npx jest --runInBand",
    "test:watch": "set NODE_OPTIONS=--experimental-vm-modules && npx jest --watch --runInBand",
    "test:cov": "set NODE_OPTIONS=--experimental-vm-modules && npx jest --no-cache --runInBand --coverage"
  },
  "keywords": [],
  "author": "fredowisk",
  "license": "ISC",
  "devDependencies": {
    "jest": "^27.1.1"
  },
  "dependencies": {
    "busboy": "^0.3.1",
    "pino": "^6.8.0",
    "pino-pretty": "^5.1.3",
    "pretty-bytes": "^5.6.0",
    "socket.io": "^4.1.3"
  }
}
